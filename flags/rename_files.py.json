import numpy as np
import pandas as pd
import os

data = pd.read_csv('../anki_data/cleaned_data.csv')
dir_path = os.path.dirname(os.path.realpath('flag-south_africa.png'))
files = os.listdir()

for f in files:
    try:
        # index = np.where(data["Capital"] == f.replace('.json', ''))[0][0]
        # futur_name = data.loc[index].short
        futur_name = f.replace('flag-', '')

        source = dir_path + '\\' + f
        dest = dir_path + '\\' + futur_name + '.json'
        print(f, dest)
        os.rename(source, dest)
    except IndexError:
        print('ERROR ', f)
